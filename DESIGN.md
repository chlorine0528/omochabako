# ゲームをつくるときのルール

娘（2023年11月13日生まれ）が、離れた場所にいる母親のスマホで遊ぶ。
毎週1本ずつ増やしていく。以下は毎回そのまま守る前提条件。

## 遊ぶ人の条件

- 文字が読めない。数字も読めない
- タップは確実にできる。ドラッグ、スワイプ、長押し、ダブルタップは当てにしない
- 集中は2〜5分。1回の体験は10秒で完結し、無限に繰り返せる形にする
- 「できなかった」が成立するとやめてしまう。負ける状態を作らない
- 手のひらや複数の指で一度に画面を押す。マルチタッチで壊れないこと

## 守ること

1. **説明をゼロにする。** 開いた瞬間に遊べる。タイトル画面もスタートボタンも置かない
2. **失敗と終了を作らない。** ゲームオーバー、残機、制限時間、スコア表示なし。何を押しても肯定的な反応を返す
3. **外れタップを作らない。** 的を外した場所を押しても何かが起きる。無反応の領域を画面に残さない
4. **当たり判定は直径68px以上。** 動く的なら余白を8px足す
5. **文字を読ませない。** 状態は色、形、動き、音で伝える
6. **画面の外に出られない。** 外部リンクなし。おもちゃばこへ戻るボタンは左上に小さく置き、700msの長押しでのみ発火する
7. **ズームとスクロールを殺す。** `user-scalable=no`、`touch-action:none`、`overscroll-behavior:none`、長押しの選択とコンテキストメニューも無効化
8. **音は最初のタップ以降に鳴らす。** AudioContext は pointerdown で resume する。iPhoneのマナーモード中は鳴らないので、その前提で音なしでも成立させる
9. **1ファイルで完結させる。** CSSとJSはインライン、画像は SVG か data: URL。外部リクエストを1本も出さない
10. **縦でも横でも成立させる。** `100dvh` を使い、`orientation` で組み替える

## 技術メモ

- canvas は `devicePixelRatio` を2で頭打ちにする。古い端末が落ちる
- 同時発音が増えると割れるので `DynamicsCompressor` を必ず挟む
- 動く要素は40個まで。それ以上は間引く
- 背景に純白と純黒を使わない。紫から青のグラデーションも使わない
- フォント指定は欧文を先、和文を後に書く

## 追加のしかた

1. `games/<slug>/index.html` を作る
2. `games.json` の先頭に1件足す（`slug` `title` `date` `color` `icon`）
3. `python3 build.py` で `index.html` を再生成する
4. コミットして push する
