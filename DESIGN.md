# ゲームをつくるときのルール

娘（2023年11月13日生まれ）が、離れた場所にいる母親のスマホで遊ぶ。
毎週1本ずつ増やしていく。以下は毎回そのまま守る前提条件。

## 遊ぶ人の条件

- 文字が読めない。数字も読めない
- タップは確実にできる。ドラッグ、スワイプ、長押し、ダブルタップは当てにしない
- 集中は2〜5分。1回の体験は10秒で完結し、無限に繰り返せる形にする
- 「できなかった」が成立するとやめてしまう。負ける状態を作らない
- 手のひらや複数の指で一度に画面を押す。マルチタッチで壊れないこと

## 守ること

1. **説明をゼロにする。** 開いた瞬間に遊べる。タイトル画面もスタートボタンも置かない
2. **失敗と終了を作らない。** ゲームオーバー、残機、制限時間、スコア表示なし。何を押しても肯定的な反応を返す
3. **外れタップを作らない。** 的を外した場所を押しても何かが起きる。無反応の領域を画面に残さない
4. **当たり判定は直径68px以上。** 動く的なら余白を8px足す
5. **文字を読ませない。** 状態は色、形、動き、音で伝える
6. **画面の外に出られない。** 外部リンクなし。おもちゃばこへ戻るボタンは左上に小さく置き、700msの長押しでのみ発火する
7. **ズームとスクロールを殺す。** `user-scalable=no`、`touch-action:none`、`overscroll-behavior:none`、長押しの選択とコンテキストメニューも無効化
8. **音は最初のタップ以降に鳴らす。** AudioContext は pointerdown で resume する。iPhoneのマナーモード中は鳴らないので、その前提で音なしでも成立させる
9. **1ファイルで完結させる。** CSSとJSはインライン、画像は SVG か data: URL。外部リクエストを1本も出さない
10. **縦でも横でも成立させる。** `100dvh` を使い、`orientation` で組み替える

## ひと工夫を必ず1つ入れる

動詞と終わらない仕掛けだけだと、3分で飽きる。
基本の手触りとは別に、驚きを1つ入れる。下の7つから選び、
使った型を `games.json` の `twist` に書く。前の週と同じ型は選ばない。

| 型 | 中身 | 例 |
|---|---|---|
| まれに別のものが出る | 同じ操作なのに、ときどき違う結果になる | はなび。10種のかたちが4回に1回 |
| ためると変わる | くり返した回数で景色が変わり、放っておくと戻る | 押すほど水がたまり、やめると引く |
| つよさの端で変わる | 連続量の端（とても速い、とてもゆっくり）でだけ別の反応になる | そっとこすると下の層が透ける |
| 2つ同時で変わる | 2か所を同時に触ったときだけ3つ目のことが起きる | 2色を同時に押すと混ざった色が出る |
| 向こうから来る | ときどき何かが画面を横切る。触っても触らなくてもよい | 鳥が飛んできて、押すと羽が落ちる |
| 景色が巡る | こちらが触らなくても、ゆっくり昼夜や天気が動く | 30秒かけて空の色が変わる |
| 隠れているものが出る | ある量までためると、見たことのないものが出る | 掘り続けると化石が出る |

工夫を入れるときに守ること。

- **操作を増やさない。** 同じ触り方のまま、結果だけが変わる。新しいジェスチャを覚えさせない
- **気づかなくても損をしない。** 出会えなかった回も、そのままで完結している
- **予告しない。** 「もうすぐ出るよ」の表示、ゲージ、残り回数を置かない
- **出たことを褒めない。** 歓声も花丸も出さない。ただ起きて、ただ終わる
- **2回続けて同じものを出さない。** 直前に出したものを覚えておいて避ける
- **10秒の体験を4〜6回くり返すあいだに1回は当たるようにする。**
  レア度を上げすぎると、2〜5分の集中では一度も出会えないまま終わる

入れたら、本当に出るかを数える。目分量で「入れました」と書かない。
確率を直接読める形にしておくか、`tools/` に確認用のスクリプトを足す。

## 技術メモ

- canvas は `devicePixelRatio` を2で頭打ちにする。古い端末が落ちる
- 同時発音が増えると割れるので `DynamicsCompressor` を必ず挟む
- 動く要素は40個まで。それ以上は間引く
- 背景に純白と純黒を使わない。紫から青のグラデーションも使わない
- フォント指定は欧文を先、和文を後に書く
- 指の接触面の大きさ（`PointerEvent` の `width` と `height`）で操作を分岐させない。
  実機では指1本でも50pxを超えることがあり、値は端末ごとに違う。ヘッドレスのブラウザは
  常に1pxを返すので、ここで分岐すると20項目を通ったまま実機だけで壊れる。
  手のひらを見分けたいときは、同時に降りたポインタの数で判断する

## 追加のしかた

1. `games/<slug>/index.html` を作る
2. `games.json` の先頭に1件足す（`slug` `title` `date` `bg` `icon` `twist`）
3. `python3 build.py` で `index.html` を再生成する
4. コミットして push する
